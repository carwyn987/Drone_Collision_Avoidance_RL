Deterministic counterpart to the reinforcement learning algorithm with actions and states.

This model attempts simply simulates taking all possible moves, and then chooses the best reward. It is necessary for the reward function to be a gradient.

